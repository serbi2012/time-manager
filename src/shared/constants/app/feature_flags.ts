/**
 * Feature flags for toggling app features on/off.
 *
 * Set `visible` to false to hide a feature from the UI
 * without removing the underlying code.
 */

export const FEATURE_FLAGS = {
    suggestions: {
        visible: false,
    },
} as const;
