# 설정

**설정** 모달에서는 데이터 관리, 단축키 설정, 자동완성 옵션을 관리할 수 있습니다.

---

## 설정 열기

| 방법 | 설명 |
|------|------|
| **설정 버튼** | 헤더 우측의 ⚙️ 아이콘 클릭 |
| **[단축키](wiki:shortcuts)** | `Alt + ,` |

---

## 설정 모달 구성

설정 모달은 네 개의 탭으로 구성됩니다:

| 탭 | 주요 기능 |
|-----|-----------|
| **일반** | 프리셋 추가 시 동작 설정 |
| **데이터 관리** | 내보내기, 가져오기, 수동 동기화 |
| **단축키** | 단축키 활성화/비활성화 |
| **자동완성** | 옵션 숨기기/복원 |

---

## 일반 탭

프리셋 관련 설정을 관리합니다.

### 작업 추가 시 구분자(postfix) 사용

[프리셋](wiki:work-preset)으로 작업을 추가할 때 거래명 뒤에 시간 구분자를 붙일지 설정합니다.

| 설정 | 동작 | 예시 |
|------|------|------|
| **OFF** (기본) | 이름 그대로 추가 | `회의` |
| **ON** | 시간 구분자 추가 | `회의_0122_093045_123` |

> **참고**: 같은 이름의 미완료 작업이 이미 있으면 자동으로 번호가 붙습니다.
> 예: `회의` → `회의 (2)` → `회의 (3)`

---

## 데이터 관리 탭

아래는 데이터 관리 패널의 모습입니다:

:::demo SettingsPanel:::

### 데이터 내보내기

모든 작업 기록과 설정을 **JSON 파일**로 내보냅니다.

| 항목 | 내용 |
|------|------|
| **파일명** | `time-manager-backup-YYYY-MM-DD.json` |
| **포함 데이터** | records, templates, timer, custom_options |

### 데이터 가져오기

백업한 JSON 파일에서 데이터를 복원합니다.

> **⚠️ 주의**: 가져오기를 하면 **현재 데이터가 덮어씌워집니다**. 필요한 경우 먼저 내보내기를 해두세요.

### 수동 동기화

로그인 상태에서 클라우드와 수동으로 동기화합니다.

| 상태 | 동작 |
|------|------|
| **로그인** | 즉시 클라우드와 동기화 |
| **게스트** | 버튼 비활성화 |

---

## 단축키 탭

키보드 [단축키](wiki:shortcuts)를 관리합니다.

### 단축키 목록

모든 단축키가 테이블 형태로 표시됩니다:

:::demo ShortcutsTable:::

### 단축키 활성화/비활성화

각 단축키의 **스위치를 토글**하여 개별적으로 활성화/비활성화할 수 있습니다.

> **활용**: 브라우저나 다른 앱의 단축키와 충돌하는 경우 비활성화

### 기본값으로 초기화

**"기본값으로 초기화"** 버튼을 클릭하면 모든 단축키가 기본 상태로 복원됩니다.

---

## 자동완성 탭

자동완성에 표시되는 옵션을 관리합니다.

### 자동완성 옵션 종류

| 필드 | 데이터 소스 |
|------|-------------|
| **작업명** | 기존 작업 기록 + 프리셋 |
| **업무명** | 기존 작업 기록 + 프리셋 |
| **거래명** | 기존 작업 기록 + 프리셋 |
| **프로젝트 코드** | 기존 작업 기록 + 프리셋 |
| **업무명 Select 옵션** | 기본값 + 사용자 추가 |
| **카테고리 Select 옵션** | 기본값 + 사용자 추가 |

### 옵션 숨기기

불필요한 옵션을 자동완성에서 숨길 수 있습니다:

1. 숨길 옵션 체크
2. **"선택 항목 숨기기"** 버튼 클릭
3. 자동완성 목록에서 해당 옵션이 사라짐

### 숨긴 옵션 복원

1. **"숨긴 항목"** 섹션 펼치기
2. 복원할 옵션 선택
3. **"선택 항목 복원"** 버튼 클릭

---

## 설정 동기화

로그인 상태에서 설정은 **클라우드에 자동 동기화**됩니다:

### 동기화되는 설정

| 설정 항목 | 동기화 |
|-----------|:------:|
| 단축키 활성화 상태 | ✅ |
| 숨긴 자동완성 옵션 | ✅ |
| 사용자 정의 업무명/카테고리 | ✅ |
| 작업 프리셋 순서 | ✅ |

---

## 기본 옵션 값

### 업무명 기본 옵션

`개발`, `작업`, `분석`, `설계`, `테스트`, `기타`

### 카테고리 기본 옵션

`개발`, `문서작업`, `회의`, `환경세팅`, `코드리뷰`, `테스트`, `기타`

> [작업 프리셋](wiki:work-preset) 또는 작업 추가 모달에서 새 옵션을 추가할 수 있습니다.

---

## 관련 문서

- **[시작하기](wiki:getting-started)**: 로그인 및 동기화 개요
- **[작업 프리셋](wiki:work-preset)**: 프리셋에서 자동완성 옵션 관리
- **[단축키 목록](wiki:shortcuts)**: 전체 단축키 목록 및 사용법
